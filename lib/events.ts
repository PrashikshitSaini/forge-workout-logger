/** Custom window event names used to coordinate loosely-coupled components. */

/** Fired after the user finishes/saves a workout, so the AI coach can refresh. */
export const DATA_CHANGED_EVENT = "forge:data-changed";
